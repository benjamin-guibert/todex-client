import styled from 'styled-components'
import Container from 'react-bootstrap/Container'

const StyledDepositWithdraw = styled(Container)`
  grid-area: deposit-withdraw;
  height: 100%;
`

export default StyledDepositWithdraw
